# Lab 1 participant worksheet

Edit only the fields named at the current checkpoint of [the participant guide](../README.md).
Everything committed to this public repository is public. Use a non-secret alias, never passwords, tokens, customer names, or private contact details.

| Field | Your non-secret value |
| --- | --- |
| Participant alias | REPLACE_WITH_YOUR_TRAINING_ALIAS |
| Owned repository | REPLACE_WITH_YOUR_OWNED_REPOSITORY_HTTPS_URL |
| Instructor starter commit | REPLACE_WITH_THE_INSTRUCTOR_CONFIRMED_STARTER_SHA |
| Automatic push note | Not edited yet |
| PR note | Not edited yet |
| Filter negative note | Not edited yet |
| Filter positive note | Not edited yet |

Record observed runs in [the evidence worksheet](evidence.md), not in these starter placeholders.
The instructor publishes on **dev**; the participant's separate owned repository defaults to **main**.
No Azure configuration, credentials, variables, or environment secrets are requested in this lab.
